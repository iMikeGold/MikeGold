# Resolver Boundary and Gap Audit

Status: bounded investigation. No final Hat vectors, personal scores, Work claims or capability events are approved by this document.

## One knowledge system, three read models

| Read model | Admission source | May explain/rank after admission | Forbidden admission inputs |
| --- | --- | --- | --- |
| Hat resolver | declared direct/core semantic coverage and explicit Hat edges | module requirement fit | prose similarity, category, tags, legacy polygon |
| Service resolver | resolved concepts, valid object/activity composition and module dependencies | capability coverage | featured state, editorial order, project prominence |
| Work resolver | confirmed contribution claim or sufficiently specific semantic signature | admitted Hat connection, environment, quality and Evidence | Hat link alone, featured state, editorial order, lead media |

The executable boundary validator scans the Service and Work retrieval implementation plus its generated indexes. Presentation fields remain valid consumers in the Work Explorer; they are forbidden only from Service/Work relevance.

## Current field-consumer map

| Field family | Current legitimate consumer | Service/Work relevance |
| --- | --- | --- |
| Project `featured` | public Project presentation and editorial Work browser | forbidden |
| lens editorial boost/sequence | Work Explorer ordering inside an explicitly chosen editorial lens | forbidden |
| Evidence lead media/sequence | Project media presentation | forbidden |
| confirmed claim concepts | Service Work candidate index and Work admission | allowed |
| derived semantic signature | lower-confidence candidate index | allowed, subject to specificity threshold |
| Hat applied-in relationship | capability explanation/boost after Work admission | cannot admit |
| legacy six-axis vector | Registry polygon visualisation only | forbidden |

## Exact gap traces

| Query | Current interpretation | Classification | Required next knowledge correction |
| --- | --- | --- | --- |
| `hardware + software integration for a product` | Systems integration; `product` unresolved; Live Systems Designer selected | missing product object and incorrect broad Hat ownership | model reusable product/physical-digital integration composition; audit Systems Engineer and integration Hat coverage |
| `birthday party DJ rig` | no concepts | missing concepts/aliases | DJ playback system, PA/audio reinforcement, small-event audio, equipment planning, setup/operation |
| `live PA system` | `live` incorrectly becomes operational state; PA/system unresolved | wrong sense plus missing object alias | PA/public-address/audio-reinforcement object must give `live` an event target |
| `live sound` | Audio + live environment; PlayStation admitted from broad derived audio signature | missing live-audio compound and incorrect Work facts | add confirmed live-sound claims for relevant Crystal Palace/Orb contributions; correct PlayStation production context |
| `field comms live gantry` | Field communications + live environment; gantry unresolved; NFL contributions confirmed | missing environment/location concept and missing FBC claim | model broadcast gantry/control-position context; add FBC claim only from confirmed history |
| `design ecommerce` / `design eccomerce` | Ecommerce storefront + Design | resolved reusable object/alias | canonical storefront object covers ecommerce, e-commerce, ecommerce site, online shop/web shop, online storefront and the observed typo; no complete-query branch |
| `scalable backend for media streaming` | Streaming platform + scalability + software engineering | incomplete module and Hat coverage | audit Backend, Platform, Media Distribution, networking/infrastructure, reliability and scale coverage |

The new generic guard treats a task containing only a broad activity plus an unresolved subject as `clarification-required` and returns no historical Work. `design unknownword`, `design nonsenseobject` and `design unrecognised object` therefore cannot reproduce a stock design portfolio list. The structured `ecommerce-storefront` record allows `design eccomerce` to resolve normally instead.

`checkout-system` and `product-catalogue` are separate component objects. Checkout composes with an existing web application without implying a whole storefront; a product catalogue remains a content/data object without implying payments. Bare `storefront` is deliberately not a digital alias because a physical retail frontage is also a valid sense.

## Stored-data finding

All 69 current Work public summaries lack field-level authored/derived provenance. The generated Work Contribution Ledger conservatively labels them `unknown-unreviewed`. It separately exposes confirmed responsibility claims, inferred semantic concepts, authored Evidence roles, Hat links and editorial fields.

The PlayStation correction is held as a pending user-authored review assertion. It has not changed a claim or score.

## Retrieval proof

The private trace retains, per returned contribution:

`query concept → matching claim/semantic signature → base admission → optional boost → provenance tier`.

Hat-only relationships retain the hard rule:

`base admission = 0 → Hat/capability/attribute boosts = 0 → excluded`.
